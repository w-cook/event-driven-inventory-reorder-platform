# Optional API proxy override when running Vite outside Aspire.
# Docker/local mode already defaults to this address.
VITE_API_PROXY_TARGET=http://localhost:8080

# Supported demo identities: viewer, operator, admin
VITE_DEMO_USER=operator